// Full name:    FilamentNetworkTracing_Monolithic_SkeletonMask_Internal_Developer
// Abbreviation: LooTrc_Mono_Skl_Int_Dev

nextflow_process {
    name "Test LOOTRC_MONO_SKL_INT_DEV"
    script "../main.nf"
    process "LOOTRC_MONO_SKL_INT_DEV"

    test("Should run LooTrc monolithic developer") {
        when {
            process {
                """
                input[0] = [
                    [id: 'test', pixel_size_xy: 0.65],
                    file(params.test_data['image'], checkIfExists: true)
                ]
                """
            }
        }
        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() },
                { assert process.out.findAll { key, val -> key.startsWith('versions') } }
            )
        }
    }
}
