// Full name:    StitchingRegistration_Chunked_Image_Internal_EndUser
// Abbreviation: Stitch_Chnk_Img_Int_EU

process {
    withName: 'STITCH_CHNK_IMG_INT_EU' {
        ext.args = [
            // fusion method
        ].join(' ')
        publishDir = [
            path: { "${params.outdir}/stitch" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
}
